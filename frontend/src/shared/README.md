# Shared Components

This directory is intended for shared UI components (e.g., Modals, specific Buttons, Inputs) that are used across multiple features to avoid repetition and enforce UI consistency across the application.
